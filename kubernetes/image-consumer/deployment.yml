apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-consumer-deployment
  labels:
    app: node-api-demo-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: node-api-demo-app
      component: image-consumer
  template:
    metadata:
      labels:
        app: node-api-demo-app
        component: image-consumer
    spec:
      containers:
        - name: image-consumer
          image: image-consumer:0.1.2
